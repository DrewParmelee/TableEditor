<Window x:Class="AOTableEditor.XmlFilePickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Open Table XML"
        Height="640"
        Width="980"
        MinHeight="480"
        MinWidth="760"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AoAppBackgroundBrush}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/AOStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="FolderText"
                   FontWeight="SemiBold"
                   Foreground="{StaticResource AoCharcoalBrush}"
                   TextTrimming="CharacterEllipsis"/>

        <Grid Grid.Row="1"
              Margin="0,10,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="110"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="110"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="File"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource AoGraphiteBrush}"
                           FontSize="11"/>
                <TextBox x:Name="FileFilterText"
                         Height="28"
                         Margin="0,4,0,0"
                         TextChanged="Filter_Changed"/>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="LOB"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource AoGraphiteBrush}"
                           FontSize="11"/>
                <ComboBox x:Name="LineOfBusinessFilter"
                          Height="28"
                          Margin="0,4,0,0"
                          SelectionChanged="Filter_Changed"/>
            </StackPanel>

            <StackPanel Grid.Column="4">
                <TextBlock Text="State"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource AoGraphiteBrush}"
                           FontSize="11"/>
                <ComboBox x:Name="StateFilter"
                          Height="28"
                          Margin="0,4,0,0"
                          SelectionChanged="Filter_Changed"/>
            </StackPanel>

            <Button Grid.Column="6"
                    Content="Clear"
                    Width="68"
                    Margin="10,18,0,0"
                    Click="ClearFilters_Click"/>
        </Grid>

        <DataGrid x:Name="FilesGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserSortColumns="True"
                  IsReadOnly="True"
                  SelectionMode="Extended"
                  SelectionUnit="FullRow"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="{StaticResource AoLineBrush}"
                  Background="{StaticResource AoSurfaceBrush}"
                  BorderBrush="{StaticResource AoLineBrush}"
                  BorderThickness="1"
                  MouseDoubleClick="FilesGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="File"
                                    Binding="{Binding FileName}"
                                    SortMemberPath="FileName"
                                    Width="2*"/>
                <DataGridTextColumn Header="Line of Business"
                                    Binding="{Binding LineOfBusiness}"
                                    SortMemberPath="LineOfBusiness"
                                    Width="*"/>
                <DataGridTextColumn Header="State"
                                    Binding="{Binding State}"
                                    SortMemberPath="State"
                                    Width="*"/>
                <DataGridTextColumn Header="New Business Effective Date"
                                    Binding="{Binding NewBusinessEffectiveDateText}"
                                    SortMemberPath="NewBusinessEffectiveDate"
                                    Width="1.5*"/>
                <DataGridTextColumn Header="Renewal Effective Date"
                                    Binding="{Binding RenewalEffectiveDateText}"
                                    SortMemberPath="RenewalEffectiveDate"
                                    Width="1.5*"/>
            </DataGrid.Columns>
        </DataGrid>

        <DockPanel Grid.Row="3"
                   Margin="0,12,0,0">
            <TextBlock x:Name="StatusText"
                       DockPanel.Dock="Left"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource AoGraphiteBrush}"/>

            <StackPanel DockPanel.Dock="Right"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="Open"
                        Width="86"
                        IsDefault="True"
                        Click="Open_Click"/>
                <Button Content="Cancel"
                        Width="86"
                        Margin="8,0,0,0"
                        IsCancel="True"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
