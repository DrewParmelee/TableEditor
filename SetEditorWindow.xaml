<Window x:Class="AOTableEditor.SetEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Set"
        Height="430"
        Width="460"
        MinHeight="320"
        MinWidth="380"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AoAppBackgroundBrush}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/AOStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition x:Name="NameLabelRow" Height="Auto"/>
            <RowDefinition x:Name="NameInputRow" Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="NameLabel"
                   Text="Set name"
                   FontWeight="SemiBold"
                   Foreground="{StaticResource AoCharcoalBrush}"/>

        <TextBox x:Name="NameTextBox"
                 Grid.Row="1"
                 Height="28"
                 Margin="0,6,0,12"/>

        <TextBlock x:Name="ValuesLabel"
                   Grid.Row="2"
                   Text="Headers"
                   FontWeight="SemiBold"
                   Foreground="{StaticResource AoCharcoalBrush}"/>

        <TextBox x:Name="ValuesTextBox"
                 Grid.Row="3"
                 Margin="0,6,0,0"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 TextWrapping="NoWrap"/>

        <TextBlock x:Name="ErrorText"
                   Grid.Row="4"
                   Margin="0,8,0,0"
                   Foreground="#D1002A"
                   TextWrapping="Wrap"/>

        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button x:Name="ActionButton"
                    Content="Update"
                    Width="86"
                    IsDefault="True"
                    Click="Update_Click"/>
            <Button Content="Cancel"
                    Width="86"
                    Margin="8,0,0,0"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
