<Window x:Class="AOTableEditor.MetadataWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:AOTableEditor"
        Title="Metadata"
        Height="330"
        Width="460"
        MinHeight="300"
        MinWidth="420"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AoAppBackgroundBrush}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/AOStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="MetadataDatePickerButtonStyle" TargetType="Button">
                <Setter Property="Width" Value="32"/>
                <Setter Property="MinWidth" Value="32"/>
                <Setter Property="Height" Value="30"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Foreground" Value="{StaticResource AoGraphiteBrush}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="ButtonBorder"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="0,4,4,0">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Border>

                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource AoHoverBrush}"/>
                                    <Setter Property="Foreground" Value="{StaticResource AoBlueBrush}"/>
                                </Trigger>

                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource AoPressedBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="MetadataDatePickerTextBoxStyle" TargetType="{x:Type DatePickerTextBox}">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="8,0"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Foreground" Value="{StaticResource AoCharcoalBrush}"/>
            </Style>

            <Style x:Key="MetadataDatePickerStyle" TargetType="DatePicker">
                <Setter Property="Height" Value="32"/>
                <Setter Property="SelectedDateFormat" Value="Short"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="DatePicker">
                            <Grid x:Name="PART_Root"
                                  SnapsToDevicePixels="True">
                                <Border x:Name="DateBorder"
                                        Background="{StaticResource AoSurfaceBrush}"
                                        BorderBrush="{StaticResource AoLineBrush}"
                                        BorderThickness="1"
                                        CornerRadius="4"/>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="32"/>
                                    </Grid.ColumnDefinitions>

                                    <DatePickerTextBox x:Name="PART_TextBox"
                                                       Grid.Column="0"
                                                       Focusable="{TemplateBinding Focusable}"
                                                       Style="{StaticResource MetadataDatePickerTextBoxStyle}"/>

                                    <Button x:Name="PART_Button"
                                            Grid.Column="1"
                                            Focusable="False"
                                            Style="{StaticResource MetadataDatePickerButtonStyle}">
                                        <TextBlock Text="&#xE787;"
                                                   FontFamily="Segoe MDL2 Assets"
                                                   FontSize="14"/>
                                    </Button>

                                    <Popup x:Name="PART_Popup"
                                           AllowsTransparency="True"
                                           Placement="Bottom"
                                           PlacementTarget="{Binding ElementName=PART_TextBox}"
                                           StaysOpen="False"/>
                                </Grid>
                            </Grid>

                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="DateBorder" Property="BorderBrush" Value="{StaticResource AoSteelBrush}"/>
                                </Trigger>

                                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                    <Setter TargetName="DateBorder" Property="BorderBrush" Value="{StaticResource AoSteelBrush}"/>
                                </Trigger>

                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.55"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Line of Business"
                   FontWeight="SemiBold"
                   Foreground="{StaticResource AoCharcoalBrush}"/>

        <ComboBox Grid.Row="1"
                  Height="28"
                  Margin="0,6,0,12"
                  ItemsSource="{Binding Source={x:Static local:MetadataWindow.LineOfBusinessChoices}}"
                  SelectedItem="{Binding LineOfBusiness, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="2"
                   Text="State"
                   FontWeight="SemiBold"
                   Foreground="{StaticResource AoCharcoalBrush}"/>

        <ComboBox Grid.Row="3"
                  Height="28"
                  Margin="0,6,0,12"
                  ItemsSource="{Binding Source={x:Static local:MetadataWindow.StateChoices}}"
                  SelectedItem="{Binding State, UpdateSourceTrigger=PropertyChanged}"/>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="New Business Effective Date"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource AoCharcoalBrush}"/>

                <DatePicker Margin="0,6,0,0"
                            Style="{StaticResource MetadataDatePickerStyle}"
                            SelectedDate="{Binding NewBusinessEffectiveDate, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Renewal Effective Date"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource AoCharcoalBrush}"/>

                <DatePicker Margin="0,6,0,0"
                            Style="{StaticResource MetadataDatePickerStyle}"
                            SelectedDate="{Binding RenewalEffectiveDate, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,18,0,0">
            <Button Content="Update"
                    Width="86"
                    IsDefault="True"
                    Click="Update_Click"/>
            <Button Content="Cancel"
                    Width="86"
                    Margin="8,0,0,0"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
