<Window x:Class="AOTableEditor.ExportTablesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Export Tables"
        Height="420"
        Width="460"
        MinHeight="320"
        MinWidth="400"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AoAppBackgroundBrush}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/AOStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Margin="0,0,0,8">
            <TextBlock Text="Select tables to export"
                       DockPanel.Dock="Left"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource AoCharcoalBrush}"
                       VerticalAlignment="Center"/>

            <Button Content="All"
                    DockPanel.Dock="Right"
                    Width="58"
                    Click="SelectAll_Click"/>
        </DockPanel>

        <ListBox x:Name="TablesList"
                 Grid.Row="1"
                 SelectionMode="Extended"
                 DisplayMemberPath="Name"
                 Style="{StaticResource TableListStyle}"
                 ItemContainerStyle="{StaticResource TableListItemStyle}"
                 MouseDoubleClick="TablesList_MouseDoubleClick"/>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Content="Export"
                    Width="86"
                    IsDefault="True"
                    Click="Export_Click"/>
            <Button Content="Cancel"
                    Width="86"
                    Margin="8,0,0,0"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
